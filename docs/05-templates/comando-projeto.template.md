---
description: "{DESCRICAO}"
argument-hint: "{ARG_HINT}"
---

Você está operando o **OML — Orquestrador de Migração Legada** (localizado em `../OML/`) para **este projeto**. Execute o comando `/{NOME}` seguindo rigorosamente `../OML/docs/01-comandos/{DOC}` e os protocolos/políticas que ele referencia.

- Trabalhe sobre **este projeto** (o diretório atual) e consulte a `memoria/` **deste projeto**.
- Cumpra as **Ações obrigatórias** e respeite as **Ações proibidas** do doc.
- Se houver gate duro ou autorização pendente, **pare** conforme o protocolo.
- Encerre com a **Frase obrigatória de encerramento** do comando.

Argumentos fornecidos (se houver): $ARGUMENTS

<!--
  TEMPLATE — gerado pelo OML em `.claude/commands/` do PROJETO NOVO (sibling do OML),
  um arquivo por comando, durante `/migracao-configurar-projeto`. Ao gerar:
    - O bloco de frontmatter (---) deve ser a PRIMEIRA coisa do arquivo (senão o
      Claude Code não reconhece o slash command). Remova este comentário.
    - {NOME}      → nome do comando sem a barra (ex.: migracao-status)
    - {DESCRICAO} → descrição curta da tabela de `comandos-oficiais.md`
    - {ARG_HINT}  → dica de argumento; REMOVA a linha argument-hint se não houver
    - {DOC}       → arquivo do doc em docs/01-comandos/ (ex.: migracao-status.md)
    - Se o diretório do OML não se chamar `OML`, troque `../OML/` pelo basename real
      de `diretorio_oml` (registrado no perfil do projeto).
  Ver docs/04-protocolos/protocolo-ativacao-permanente.md
-->
